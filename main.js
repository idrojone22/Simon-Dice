let colores = [amarillo, rojo, verde, azul];

console.log(colores);

function jugar(){
    colores[0].style.filter = "brightness(300%)";
};


console.log( colores[0].style.filter = "brightness(300%)");

colores[0].style.filter = "brightness(300%)"
